$("#details").html("<%= escape_javascript(render @selected) %>");
var history = $('#history').attr('data-load');
$.get(history);
$('.log-content').filter(':visible').each(function() {
  var path = $(this).attr('data-url');
  $(this).load(path);
});
console.log('fetch tc');
